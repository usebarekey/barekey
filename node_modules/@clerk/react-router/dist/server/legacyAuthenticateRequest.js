// src/server/legacyAuthenticateRequest.ts
import { AuthStatus, constants } from "@clerk/backend/internal";
import { handleNetlifyCacheInDevInstance } from "@clerk/shared/netlifyCacheHandler";
import { clerkClient } from "./clerkClient.js";
import { patchRequest } from "./utils.js";
async function legacyAuthenticateRequest(args, opts) {
  const { request } = args;
  const { audience, authorizedParties } = opts;
  const { apiUrl, secretKey, jwtKey, proxyUrl, isSatellite, domain, publishableKey, machineSecretKey } = opts;
  const { signInUrl, signUpUrl, afterSignInUrl, afterSignUpUrl } = opts;
  const requestState = await clerkClient(args).authenticateRequest(patchRequest(request), {
    apiUrl,
    secretKey,
    jwtKey,
    proxyUrl,
    isSatellite,
    domain,
    publishableKey,
    machineSecretKey,
    audience,
    authorizedParties,
    signInUrl,
    signUpUrl,
    afterSignInUrl,
    afterSignUpUrl
  });
  const locationHeader = requestState.headers.get(constants.Headers.Location);
  if (locationHeader) {
    handleNetlifyCacheInDevInstance({
      locationHeader,
      requestStateHeaders: requestState.headers,
      publishableKey: requestState.publishableKey
    });
    throw new Response(null, { status: 307, headers: requestState.headers });
  }
  if (requestState.status === AuthStatus.Handshake) {
    throw new Error("Clerk: unexpected handshake without redirect");
  }
  return requestState;
}
export {
  legacyAuthenticateRequest
};
//# sourceMappingURL=legacyAuthenticateRequest.js.map