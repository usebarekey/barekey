---
title: "InvalidQueryFactoryError"
description: "Trace a generated Query client value that has no callable transport shape."
---

## What this error means and what can trigger it

`InvalidQueryFactoryError` means the client adapter received neither a callable native factory nor a non-null object with a callable `ts load` method. The error is raised while the adapter is created, usually during evaluation of the generated client module.

This usually means a SvelteKit-generated export is missing or incompatible, generated code is stale, or the compiler and runtime disagree about the boundary. Calling SER's internal adapter with the wrong value can also reach this error, although application code should not call that adapter directly.

One wrinkle is that the current message mentions objects exposing `ts query` or `ts load`. In 4.0.0, however, the implementation accepts a direct function or an object with `ts load`; an object exposing only `ts query` still fails. The behavior described here follows the implemented predicate.

## Exactly where it triggers

```ts name="modules/svelte-effect-runtime/src/remote/client/query.ts" icon="ts" line="90" char="9" link="https://github.com/usebarekey/svelte-effect-runtime/blob/55b22b5afcfecf1a5643c5e26f7088fae6a22a6e/modules/svelte-effect-runtime/src/remote/client/query.ts#L90"
const load = has_method(native_factory, "load")
	? native_factory.load.bind(native_factory)
	: undefined;
const query = typeof native_factory === "function" ? native_factory : undefined;

if (!query && !load) {
	throw new InvalidQueryFactoryError();
}
```

`ts has_method` requires a non-null object and a callable property. The throw is synchronous and occurs before the wrapped Query effect is returned. Generated code calls this adapter from `name="modules/svelte-effect-runtime/src/compiler/remote-client.ts" line="129" char="2" link="https://github.com/usebarekey/svelte-effect-runtime/blob/55b22b5afcfecf1a5643c5e26f7088fae6a22a6e/modules/svelte-effect-runtime/src/compiler/remote-client.ts#L129"`.

For reference, the class itself is declared at `name="modules/svelte-effect-runtime/src/errors.ts" line="587" char="14" link="https://github.com/usebarekey/svelte-effect-runtime/blob/55b22b5afcfecf1a5643c5e26f7088fae6a22a6e/modules/svelte-effect-runtime/src/errors.ts#L587"`.

## Why this is the case

Every later Query execution must choose one concrete operation: call the factory with the input, or call its bound `ts load` method. A value satisfying neither branch cannot initiate a request.

Checking during adapter construction turns a deterministic integration mismatch into a module-level diagnostic. Deferring it until the Query effect runs would make the failure dependent on user interaction and obscure which generated boundary supplied the malformed value.
